#include "main.h"
#include <string.h>

/**
 * _strlen - Calculates the length of a string.
 * @s: The pointer of the string.
* Return: The lenght of the string.
 */

int _strlen(char *s);
{
	return (strlen(*s));
}

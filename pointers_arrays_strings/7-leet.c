#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * leet - Encodes a string into 1337.
 * @s: Input String.
 *
* Return: The pointer to the encoded string.
 */

char *leet(char *s)
{
	int i = 0;
	int len = strlen(s);

	while (i <= n)
	{
		
	}
}
